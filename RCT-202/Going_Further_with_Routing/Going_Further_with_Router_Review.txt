1. The _ object can be used to programmatically change the URL.
- history

2. Given this route:
<Route path="dogs/:breed" component={Dogs} />
How do you access the value of the code>:breed parameter in a component?
- match.params.breed

3. Is this a valid route declaration?
<Route component={Footer} />
- Yes. This renders the Footer component no matter what which URL path is active.

4. Which component renders only the first <Route> that matches the URL and helps create 404-like routes?
- <Switch>

5. You can give a URL parameter any name, as long as you include a __ in front of it.
- :